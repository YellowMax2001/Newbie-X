# If we have some **build dependency**,
# **must put the dependency items in the top position**.
CompileTargetLists += $(TOP_DIR)/MaxCLib/algorithm
CompileTargetLists += $(TOP_DIR)/MaxCLib/data_structs
CompileTargetLists += $(TOP_DIR)/MaxCLib/streams
CompileTargetLists += $(TOP_DIR)/MaxCLib/
